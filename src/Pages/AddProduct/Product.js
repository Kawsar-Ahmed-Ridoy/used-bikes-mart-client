import React from 'react';

export const Product = ({product}) => {
    console.log(product);
    return (
        <div className='text-4xl'>
            kile bela
        </div>
    );
};

export default Product;